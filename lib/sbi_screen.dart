import 'package:flutter/material.dart';

class SBIScreen extends StatelessWidget {
  const SBIScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SBI Screen'),
    );
  }
}
